		MASTER NODE

mkdir -p /data
ls -alrt /data/

sudo apt install nfs-kernel-server
sudo chown nobody:nogroup /data/
sudo chmod 777 /data/

sudo vi /etc/exports
 paste the last line ------ /data *(rw,sync,no_root_squash)
sudo exportfs -rav

sudo systemctl restart nfs-kernel-server



         worker 1and 2 install
		 
sudo apt install nfs-common

sudo mkdir -p /data
sudo mount 172.31.34.180:/data /data 
ls /data


